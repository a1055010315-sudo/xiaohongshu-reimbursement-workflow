interface:
  display_name: "三类报销局部增量工作流"
  short_description: "动态生成本批报销成品、局部候选和双门禁预览"
  default_prompt: "使用 $xiaohongshu-reimbursement-workflow 处理本批小红书、公司或驻所的普通报销或补报；使用脱敏模板生成当前批成品，不检查历史全表业务，只生成本批局部候选和预览，并经两道门禁安全发布。"
policy:
  allow_implicit_invocation: false
