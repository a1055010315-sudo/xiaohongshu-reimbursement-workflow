# S00 交接

状态：`complete`

- Unit：`S00`
- branch：`s00-baseline`
- worktree：`C:\Users\a1055\plugins\development\xiaohongshu-finance-disbursement-vnext`
- base：marketplace `0.5.0+codex.20260822170308`；纯导入提交 `f4e1c0099872171d71838f65e796d1cd5e86126b`
- HEAD：tag `baseline/s00-v0.5.0-codex.20260822170308` 的 peeled commit；使用 `git rev-parse baseline/s00-v0.5.0-codex.20260822170308^{}` 解析
- cache 修正提交：`90da41096913bb189ae8869898cc165d111fa185`
- thread_compression_count：`0`
- rollover_generation：`0`

## 改动

1. 从指定 marketplace 完整导入 88 个文件并初始化 Git；首提交只含纯 marketplace 字节。
2. marketplace 插件子树与安装缓存均为 85 个文件；全树逐文件 SHA-256 比较只有两处差异。
3. 第二提交只将 cache 中以下两个文件原样吸收：
   - `skills/xiaohongshu-reimbursement-workflow/scripts/audit_full_correspondence.mjs`
   - `skills/xiaohongshu-reimbursement-workflow/scripts/build_reimbursement_artifacts.mjs`
4. 两处修正合计 53 insertions、20 deletions：输出和审计将“实报/对公已付不实报”与“有截图/无截图”组成独立 F 列属性，并按属性连续段合并 F 列；D:E 仍按费用组连续段合并。
5. 新增的其他文件仅为 `docs/implementation` 状态、契约占位和交接文档。

## 差异证据

| 文件 | marketplace SHA-256 | cache SHA-256 |
| --- | --- | --- |
| `audit_full_correspondence.mjs` | `f32825cc7e64996b48f78637f0574150b602e041437845daf7c05166548b7d70` | `4c92eae92dfc9869aa15f3b3d25cbb41792a63699a24469fb48d6b913d5f6ded` |
| `build_reimbursement_artifacts.mjs` | `0fd1752bf64bcfb4eba285c1be010b30be23f8590dbfc34186885cc2af8bed6a` | `eed56afcdb3ff0446e4c96066462126bb02065efb0f43f01e9fa109c53638d0f` |

无 marketplace-only 或 cache-only 文件。

## 测试

- Codex bundled Node：`v24.19.0`
- 命令与临时运行视图：见 `../contracts/test-matrix.md`
- 真实计数：38
- 结果：38 pass，0 fail，0 skipped
- 初次直接运行只有 7 项进入 runner：4 pass、3 个文件因 bundled 依赖根布局不匹配而加载失败。
- 首个 Junction 指向 bundled `bin/node_modules` 后仍为 4/7，错误收敛为缺少 `jszip`。
- Junction 改指 primary runtime 完整依赖根后为 37/38，唯一失败是 fixture 找不到 bundled Python/pypdf；设置测试原生支持的 `XHS_BUNDLED_PYTHON` 固定路径后最终 38/38。
- 上述中间失败和最终通过均为真实运行结果；没有为通过测试修改业务实现。

## 未实施

- 未取消内部 JSON 启动门槛。
- 未修改 manifest、runner、archive 业务逻辑。
- 未更新版本/cachebuster，未打包、未安装、未提速。

## 不变量与临时清理

- 来源 marketplace、安装缓存和 Codex 配置在前后复核中保持不变。
- 测试产生的临时 Node 硬链接、两个 `node_modules` junction 和临时目录已删除。
- 测试自身使用的临时 fixture 由现有测试清理；仓库内无残留临时文件。
- 真实财务目录 `C:\Users\a1055\Desktop\luna\04_财务台账`：零读取、零枚举、零搜索、零修改。

## 下一步

S01 在开始任何实现前，应从 baseline tag 创建自己的 branch/worktree，接受明确的路径所有权，并冻结其实际负责的契约部分。manifest v2 当前仍为 `UNFROZEN`，不得由实现猜测补全。

## 已知风险

- Codex 本地 bundled Node 的 manifest 将依赖列在 `bin/node_modules`，而来源 CJS 加载器从 Node 父级寻找；基线测试因此需要已记录的临时运行视图。S00 未改变此行为。
- 本仓库使用本地 Git 身份 `Codex <codex@local>`，因为环境没有全局 Git 身份；该设置仅存在于新仓库的 `.git/config`。
