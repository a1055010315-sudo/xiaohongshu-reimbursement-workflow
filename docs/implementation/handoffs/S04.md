# S04 集成交接

状态：`complete`

- owner：S04 integration
- branch：`s04-integration`
- worktree：`C:\Users\a1055\plugins\development\xhs-worktrees\S04-integration`
- base：`22f5778`
- HEAD：`S04_INTEGRATION_COMMIT`（本交接所在提交，以交付的 `git rev-parse HEAD` 为准）
- 已集成：S02 `dfe3a9537431c2a6317a25a7701b4347f8bbcb86`、S03 `83f538928be067e270b04deb93c2bb9245c2a706`
- thread_compression_count：`1`
- rollover_generation：`0`

## 当前范围

- 将冻结的 manifest v2 合同接入现有 manifest auditor 与 compact runner。
- 集成 S02 published reimbursement archive auditor 和 S03 fresh reimbursement/salary auditor。
- 保持 v1 行为兼容，并提供可消费的弃用 warning。
- 不修改 archive publisher、PDF worker、普通报销 runner/auditor、版本、打包或主文档。
- 不减少双次来源审计、candidate/stage/final 审计、TOCTOU、fsync、原子发布或恢复保护。
- archive request 外层仍严格为四字段，CLI 仍仅接受 `--archive`。

## 已完成

- 修正 S02 receipt digest，使其基于 publisher 实际使用的、排除 `receiptDigest` 与后置 `cleanup` 的 receipt core。
- 收紧 published archive receipt 的顶层、角色路径/fileId 一一闭合和 original manifest no-follow attestation。
- `auditDisbursementManifest` 已按 exact kind/version 分派 v1/v2；v1 进入原 auditor 并返回 `DISBURSEMENT_MANIFEST_V1_DEPRECATED` warning。
- 新增 `disbursement_manifest_v2_auditor.mjs`，接入 S01 validator、S02 published auditor 与 S03 fresh/salary auditor。
- v2 复用现有 v1 业务闭合尾段，保留 rows、状态、金额、ref、voucher、expected、尾差和工资 gross 规则。
- v2 要求所有 `sourceFiles` ID 都实际进入 reimbursement、salary 或 voucher 的稳定读取/hash/type 验证，并将路径加入 `runtime.boundSourcePaths`。
- runner 已将首次 manifest audit、bound paths 和 archive parent identity 捕获提前至 workflow owner 创建之前。
- production fixture 已支持 v1/v2、fresh/published reimbursement、salary-only、mixed 与可选 attestations。
- 已用临时诊断确认 v2 fresh mixed、v2 published reimbursement audit，以及 v2 salary-only 完整 archive/最终验证/cleanup 能成功。
- production E2E 已加入 v2 reimbursement-only、salary-only、mixed，以及 published archive 四个场景。
- invalid manifest/source 在 workflow owner 创建前完成首审；旧 PDF/owner 回归已同步为“不创建 workflow root”和“有效但不同 manifest path”的精确断言。

## 当前修改

- `scripts/disbursement_manifest.mjs`
- `scripts/disbursement_manifest_v2_auditor.mjs`（新增）
- `scripts/disbursement_reimbursement_source_v2.mjs`
- `scripts/run_compact_disbursement_workflow.mjs`
- `tests/disbursement-production-fixture.mjs`
- `tests/disbursement-production-e2e.test.mjs`
- `tests/disbursement-pdf-security.test.mjs`
- `tests/disbursement-reimbursement-published-archive-v2.test.mjs`
- `docs/implementation/handoffs/S04.md`

## 最终测试

- 修改/新增 `.mjs` 全部 `node --check` 通过；`git diff --check` 通过。
- v2 三组专属测试合跑：80 pass，0 fail，0 skipped。其中 contract 33、fresh source 23、published archive 24。
- 新增最小 production E2E：fresh reimbursement-only、salary-only、mixed 和无原 manifest/receipt 的 published archive 四类均通过；Node runner 计数 5 pass（含三个嵌套子测试）。
- S00 原四文件发放回归组合：43 pass，0 fail，0 skipped；即历史 38 项全部保留通过，并加入上述 5 个 v2 runner 测试。
- 完整回归使用 bundled Node `v24.19.0`、primary runtime `pdfjs-dist`，并通过 `XHS_BUNDLED_PYTHON` 固定到 bundled Python/pypdf；加密 PDF 实链路通过。

## 限制与不变量

- 未修改 archive publisher、PDF worker、普通报销 runner/auditor、版本、打包或主文档。
- 未实施整体提速，也未减少双次来源审计、candidate/stage/final 审计、TOCTOU、fsync、原子发布或恢复保护。
- fresh salary workbook/image 继续诚实标记 `review_bound_no_frozen_salary_schema` / `review_bound_no_ocr`；没有提升为 artifact-parsed。
- 真实财务目录保持零读取、零枚举、零搜索、零修改。
- 仓库根 `.tmp-s04-codex-node` 仅为本任务测试运行视图，提交前安全清理，不进入 Git。

## 下一动作

无；清理临时运行视图并提交本集成单元。
