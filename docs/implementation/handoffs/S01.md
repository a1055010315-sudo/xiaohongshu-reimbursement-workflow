# S01 交接

状态：`complete`

- Unit：`S01`
- owner：contract unit
- branch：`s01-contract`
- worktree：`C:\Users\a1055\plugins\development\xhs-worktrees\S01-contract`
- base：`baseline/s00-v0.5.0-codex.20260822170308`（peeled `8a09a77c4a7cef2845e4bff8825162b620544ecc`）
- HEAD：`S01_CONTRACT_COMMIT`（本交接所在提交，以 `git rev-parse HEAD` 的交付值为准）
- dirty files：提交后无
- thread_compression_count：`0`
- rollover_generation：`0`

## 已完成

1. 冻结 `disbursement-archive-manifest-v2` / version 2 exact schema。
2. 新增独立纯结构模块 `scripts/disbursement_manifest_v2_contract.mjs`；现有 v1 `disbursement_manifest.mjs` 零修改。
3. `sourceFiles` 成为唯一来源 path/SHA/kind/usage 注册表，所有来源、工资和凭证只用 fileId 引用。
4. 报销来源 mode 只允许 `published_archive` / `fresh_evidence`。
5. 原 manifest、publish receipt、salary certificate 变为可选 attestation；缺失不阻塞，提供时必须有合法注册表绑定并被 task-internal review 覆盖，后续必须 fresh 验证。
6. 冻结内嵌 `sourceReview`：`producer=task_internal`，每个报销/工资来源恰好一个完整 review，不能改为用户 sidecar。
7. rows/expected 保持 v1 业务形状；S01 只做 exact/type/id/ref/重复/金额与日期原始格式，不做文件读取和业务闭合。
8. 冻结 kind/version-only dispatcher；不按 body 猜版本，不允许 v1/v2 混用。

## 测试

命令：

```powershell
node --test --test-reporter=tap `
  plugins\xiaohongshu-reimbursement-workflow\skills\xiaohongshu-reimbursement-workflow\tests\disbursement-manifest-v2-contract.test.mjs `
  plugins\xiaohongshu-reimbursement-workflow\skills\xiaohongshu-reimbursement-workflow\tests\disbursement-domain.test.mjs
```

结果：37 pass，0 fail，0 skipped（v2 contract 33；现有 domain 4），104.9305 ms。

## 给 S02 的接口

- 输入：已通过 `validateDisbursementManifestV2` 的 `sourceFiles/reimbursementSources/sourceReview.reimbursement`。
- 按 mode fresh 读取每个 review item 的全部 `reviewedFileIds`，重建 `facts.transactions`。
- `published_archive` 不再强制原 manifest/receipt；`fresh_evidence` 不需要伪造发布链。
- 任一可选原 manifest/receipt 一旦出现，必须核字节、SHA、kind/usage、内容和事实一致性；不能静默忽略。
- 输出独立核对结果给 S04；不得在 S02 暗改 schema 或把业务解析塞回 S01 validator。

## 给 S03 的接口

- 输入：已通过结构验证的 `sourceFiles/salaryArtifacts/sourceReview.salary`。
- fresh 解析 final workbook/image，重建 `facts.payments` 与 `grossPayTotal`。
- salary certificate 不再是启动门槛；一旦提供，必须同时验证并与 final artifact/review facts 闭合。
- 输出独立核对结果给 S04；不得只信 certificate 或 manifest 元数据。

## S04 接续

严格先调用 `dispatchDisbursementManifestVersion`；v1 仍进现有 auditor，v2 先过结构 validator，再汇合 S02/S03。S04 负责交易覆盖、工资 payments 到 rows、金额/expected、状态、尾差和文件安全业务闭合。不得按字段猜版本。

## 明确未做

- 未修改 v1 auditor、compact runner、archive、生产 fixture、普通报销 Gate 1/Gate 2。
- 未读取/解析正式归档、图片或工资内容；未接入 archive；未更新版本、cachebuster、打包或安装。
- 未实施候选并发、PDF worker、审计次数或任何整体提速。
- 真实财务目录保持零读取、零枚举、零搜索、零修改。

## 风险

- S01 只证明 JSON 结构与引用闭合；sourceReview 事实真实性必须由 S02/S03 fresh 验证，业务闭合必须由 S04 完成。
- `archiveParentPath` 是输出目录、`storeReference` 是逻辑引用；二者不是来源文件路径。所有可读取来源文件仍必须在 `sourceFiles`。
- 若后续发现 schema 必须变更，应显式重开 contract 所有权并补契约测试，不能在 consumer 内做隐式兼容。
