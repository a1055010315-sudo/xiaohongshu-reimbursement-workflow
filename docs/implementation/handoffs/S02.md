# S02 交接

状态：`complete`

- Unit：`S02`
- owner：published archive reimbursement source unit
- branch：`s02-published-archive`
- worktree：`C:\Users\a1055\plugins\development\xhs-worktrees\S02-published-archive`
- base：`S01_CONTRACT_COMMIT` `493c702b092d88562b79f4e89e5a08c26226a222`
- HEAD：`S02_COMMIT`（本交接所在提交，以交付的 `git rev-parse HEAD` 为准）
- dirty files：提交后无
- thread_compression_count：`0`
- rollover_generation：`0`

## 所有权与改动

本单元只新增：

- 生产模块：`plugins/xiaohongshu-reimbursement-workflow/skills/xiaohongshu-reimbursement-workflow/scripts/disbursement_reimbursement_source_v2.mjs`
- 专属合成测试：`plugins/xiaohongshu-reimbursement-workflow/skills/xiaohongshu-reimbursement-workflow/tests/disbursement-reimbursement-published-archive-v2.test.mjs`
- 本交接：`docs/implementation/handoffs/S02.md`

未修改 S01 contract、`disbursement_manifest.mjs`、runner、archive builder、共享 fixture、普通报销 Gate 1/Gate 2、版本、cachebuster、marketplace、安装缓存或 Codex 配置。

## 已完成

1. 新增 `auditDisbursementReimbursementSourcesV2({ sourceFiles, reimbursementSources, reimbursementReviews })`。输入必须是 `validateDisbursementManifestV2` 已规范化的三个对应切片；本模块只接受 `published_archive`，不处理 `fresh_evidence`。
2. 只读取每个 source 的 `inputFileIds` 和显式 attestation file IDs；不枚举或扫描父目录。receipt/原 manifest 中出现的文件路径也必须先映射到这组显式来源，否则拒绝。
3. 每个来源文件均执行非链接普通文件身份、稳定读取、大小与 SHA-256 绑定。workbook 用现有 `readWorkbookOoxmlFacts` 安全解析；text/JSON 严格 UTF-8；image 按 magic、尺寸与完整像素 decode 验证。
4. 正式归档 allowlist 基于现有生成文件名、目录、sheet 与结构识别：报销文字说明、报销明细、截图对应表、截至期间的总表快照、按人补报表、归档图片；receipt 显式出现时可额外列出正式根表。unknown、重复单角色或缺少核心角色均拒绝。
5. 从明细工作簿机械重建日期、人员和实报金额：F 列为“实报”时取 C 列，F 列为“对公已付不实报”时实报为 0；与 `sourceReview.facts.transactions` 按日期/人员/实报金额逐笔闭合，并绑定 detail row。
6. 补报表与明细逐行闭合；期间前交易必须有补报表。summary 的人员实报、实报合计、对公合计、缺图说明和补报说明闭合；snapshot 必须在目标 profile sheet 中包含明细的日期/项目/来源金额/人员/分类；截图行、anchor、relationship、embedded media 与显式归档图片按 transaction ID/行列/SHA 闭合。
7. 原 manifest 和 receipt 均为独立可选 attestation：缺失允许；提供时 fresh 读字节、核 SHA/内部 digest/批次/期间/人员/金额/产物绑定，损坏或冲突失败。原 manifest 继续调用现有普通 manifest auditor，但其 `files[].path` 必须全部已在本 source 显式列出，避免 attestation 扩大读取范围。
8. 输出包含每 source 的 `transactions`、`transactionBindings`、`artifactBindings`、`boundSourcePaths`、`boundSourceDigests`、attestation 结果、`sourceDigest`，并提供顶层扁平结果与 `auditDigest`；不读取或生成用户 sidecar。

## S04 接口

```js
const result = await auditDisbursementReimbursementSourcesV2({
  sourceFiles: normalized.sourceFiles,
  reimbursementSources: normalized.reimbursementSources.filter((item) => item.mode === "published_archive"),
  reimbursementReviews: normalized.sourceReview.reimbursement.filter((item) => item.mode === "published_archive"),
});
```

S04 可按 `sourceId + transactionId` 使用 `result.transactions` 与 rows 闭合；用每个 source 的 `artifactBindings`、`boundSourcePaths`、`boundSourceDigests` 做来源/归档绑定。不要把 `reviewBoundFacts` 误写成文件独立证明。

## Review-bound 与限制

- 正式归档字节不能独立推出 `batchId`；无图片交易也不能从 screenshot anchor 推出 transaction ID。两者保留 `sourceReview` authority，并在 `reviewBoundFacts` 明示。
- summary 批注、对公 summary label 的精确分组不在 v2 review 交易形状中；模块只证明可从文件闭合的金额/结构，不伪造额外语义。
- 补报原因能从正式补报表/summary 读取，但 v2 review 没有对应字段，因此标为 `published_archive_file_only`，不声称被 review 交叉证明。
- 普通生成明细的 C 列是 `sourceAmount`，不是独立的逐笔 `reimbursementAmount`。本模块仅在 F 列“实报”时将 C 作为实报额；对公不实报为 0。若未来允许“实报行 C 与 reimbursementAmount 不同”，现有正式归档无法逐笔机械分配差额，必须拒绝或重开契约，不能猜测。
- 为遵守“只读显式文件”，普通 manifest attestation 若引用未列入本 source 的历史原材料会失败；不得为了兼容而追随 manifest 路径扩大读取范围。

## 测试

专属合成正式归档使用真实 artifact builder 格式，并额外生成合成 snapshot；未使用真实财务数据。

```powershell
& '.\.tmp-s02-codex-node\bin\node.exe' --test --test-reporter=spec `
  'plugins\xiaohongshu-reimbursement-workflow\skills\xiaohongshu-reimbursement-workflow\tests\disbursement-reimbursement-published-archive-v2.test.mjs'
```

结果：23 pass，0 fail，0 skipped，约 1.53 s。覆盖：无 manifest/receipt 成功与稳定 digest；核心角色/条件补报表缺失；duplicate/unknown；SHA、人员、日期、金额、期间冲突；截图/归档凭证引用不闭合；manifest-only、receipt-only、both；损坏 digest 与 attestation 冲突。

必要回归：

```powershell
& '.\.tmp-s02-codex-node\bin\node.exe' --test --test-reporter=spec `
  'plugins\xiaohongshu-reimbursement-workflow\skills\xiaohongshu-reimbursement-workflow\tests\disbursement-manifest-v2-contract.test.mjs' `
  'plugins\xiaohongshu-reimbursement-workflow\skills\xiaohongshu-reimbursement-workflow\tests\disbursement-domain.test.mjs' `
  'plugins\xiaohongshu-reimbursement-workflow\skills\xiaohongshu-reimbursement-workflow\tests\workbook-ooxml-facts.test.mjs'
```

结果：102 pass，0 fail，0 skipped，约 1.30 s。

测试使用 worktree 内 `.tmp-s02-codex-node` 临时运行视图：Node 为已记录 bundled Node 的硬链接，`node_modules` Junction 指向 primary runtime `dependencies/node/node_modules`。测试完成后运行视图已清理，未提交。

## 不变量

- 未读取、枚举或搜索真实财务目录。
- 未接入 v1/v2 dispatcher、runner 或最终归档发布。
- 未实施性能优化、候选并发或 PDF 相关改动。
- 测试只创建合成临时材料并在结束时清理；仓库提交不含 dependency Junction 或其他临时文件。
