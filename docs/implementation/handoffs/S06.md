# S06 发布候选交接

状态：`in_progress`

- Unit：`S06`
- owner：主对话（集成分支唯一 writer）
- read-only reviewer：`s06_release_preflight`，`GPT-5.6-sol / xhigh`
- branch/worktree：`vnext/integration` / `C:\Users\a1055\plugins\development\xiaohongshu-finance-disbursement-vnext`
- base：`cda49e30bfbc88c5c14d170cfc75ee1d8e0c9ba7`
- candidate version：`0.5.0+codex.20260823144931`
- dirty files：cachebuster 与本 S06 记录；以 `git status --short` 为准
- thread_compression_count：`1`
- rollover_generation：`0`

## Goal

生成只包含“取消发放核销启动门槛”的可验证 marketplace 发布候选。不得夹带候选复用、文件并发、PDF Worker、减少审计次数等性能优化；不得改变普通报销 Gate 1/Gate 2；不得读取真实财务目录；不得修改或安装到当前 marketplace、安装缓存或 `config.toml`。

候选只允许包含：

```text
.agents/
plugins/
AGENTS.md
README.md
```

发布候选必须有独立 `.sha256` sidecar，并在全新解压目录重新验证。未获得单独明确安装授权前，只能生成和验证，不能运行插件安装、marketplace add/upgrade 或手工覆盖 cache。

## 已完成的实现证据

- S00-S05 均已集成到 `vnext/integration`；预发布代码 HEAD 为 `cda49e3`。
- v2 支持 `published_archive` 与 `fresh_evidence`；原 manifest、receipt、salary certificate 缺失可继续，已提供但错误或冲突时阻塞。
- 严格 v1 继续支持并返回 `DISBURSEMENT_MANIFEST_V1_DEPRECATED`。
- v1/v2 等价业务最终三项文件逐字节一致。
- owner-first 恢复、非法新 token 不留恢复目录、voucher 媒体类型绑定、多 profile 共享归档、零图片归档、报销来源金额差额和 no-follow attestation 均已覆盖。
- 集成树测试：523 pass，0 fail，2 skip；Skill 与 Plugin validator 通过。
- 普通报销 Gate 生产路径相对 S00 基线无差异；唯一普通 auditor 邻近修正是 deferred attestation 的真正 no-follow，不改变 Gate 语义。
- 性能实现文件相对 S00 基线无差异，没有实施整体性能优化。

## 允许修改

- `plugins/xiaohongshu-reimbursement-workflow/.codex-plugin/plugin.json`：只允许 plugin-creator helper 生成的单一 cachebuster 语义变化，以及必要的换行规范化。
- `README.md`：记录真实存在的候选版本与未安装状态，不虚构远程 Release。
- `docs/implementation/**`：记录所有权、测试、候选 SHA、验证和交接。
- 开发仓库内新的 `dist/`：只放最终 ZIP 和其 SHA256 sidecar；不提交真实数据或运行时临时目录。

## 验收清单

- [ ] cachebuster 只保留一个 `+codex.<token>`，base version 仍为 `0.5.0`。
- [ ] `git diff --check`、生产 `.mjs` 的 `node --check`、Skill validator、Plugin validator 通过。
- [ ] 当前源树回归通过，两个已知 skip 有明确原因。
- [ ] ZIP 顶层和完整文件清单严格闭合，没有 `.git`、`docs`、`dist`、测试临时目录、恢复目录或 Junction/reparse。
- [ ] ZIP SHA256 与 sidecar 相同，解压后 plugin version 与候选版本一致。
- [ ] 解压候选再次通过 marketplace/Plugin/Skill 校验和必要静态检查。
- [ ] 包内不存在真实财务数据、绝对真实财务路径、任务临时 manifest/request、恢复 owner 或秘密。
- [ ] 来源 marketplace、安装 cache 和 `config.toml` 的只读指纹与 S06 开始时一致。
- [ ] 独立只读 reviewer 的发现已关闭或明确记录。
- [ ] 最终候选路径、SHA256、source commit、测试结果和未安装状态写回本文件。

## 压缩规则

- 第一次压缩已发生：`thread_compression_count=1`。已暂停新增设计并重新核对 Goal、契约、branch、HEAD、diff 和测试。
- 第二次压缩：改为 `thread_compression_count=2`、状态 `needs-successor`；停止新增设计，记录安全接续点、HEAD、dirty files、测试、候选 SHA 与下一动作，换继任对话。
- 继任沿用 Unit、branch/worktree，名称增加 `R1/R2`，`rollover_generation+1`，压缩计数归零。

## 下一动作

完成 cachebuster/记录提交，运行当前树验证，构建并解压复验 ZIP；随后更新本 handoff 为 `complete`。
