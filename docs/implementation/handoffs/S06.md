# S06 发布候选交接

状态：`complete`

- Unit：`S06`
- owner：主对话（集成分支唯一 writer）
- independent reviewer：`s06_release_preflight`，`GPT-5.6-sol / xhigh`，最终结论 `GO`
- branch/worktree：`vnext/integration` / `C:\Users\a1055\plugins\development\xiaohongshu-finance-disbursement-vnext`
- candidate source commit：`9e9b52142020433af7092c6788562720d766bda6`
- candidate source tree：`d3f2f186fe9d6620ec634493f197bf5f4fe7c804`
- documentation closure：本文件所在的后续 docs-only 提交；不得据此重建或改变候选字节
- candidate version：`0.5.0+codex.20260823144931`
- thread_compression_count：`1`
- rollover_generation：`0`

## Goal 与结果

已生成只包含“取消发放核销启动门槛”的可验证 marketplace 发布候选。没有实施候选复用、文件并发、PDF Worker、减少审计次数等性能优化；普通报销 Gate 1/Gate 2 仍保留；真实财务目录未读取；当前 marketplace、安装缓存和 `config.toml` 未修改；候选未安装。

v2 支持 `published_archive` 与 `fresh_evidence`。原报销 manifest、publish receipt 和 salary certificate 缺失不再阻塞启动；任何已提供证明仍 fresh 严格验证，错误或冲突阻塞。严格 v1 继续兼容一个发布周期并返回 `DISBURSEMENT_MANIFEST_V1_DEPRECATED`。

## 固定候选

- ZIP：`C:\Users\a1055\plugins\development\xiaohongshu-finance-disbursement-vnext\dist\xiaohongshu-finance-0.5.0+codex.20260823144931.zip`
- sidecar：`C:\Users\a1055\plugins\development\xiaohongshu-finance-disbursement-vnext\dist\xiaohongshu-finance-0.5.0+codex.20260823144931.zip.sha256`
- SHA256：`94f6de78b2404087ca724b0f3df9ba5b1848558e0972e28adc02b1f86af578e9`
- ZIP bytes：`661561`
- ZIP entries：`111`，其中文件 `96`
- 文件解压后总字节：`2549958`
- 构建方式：从固定 source commit 执行 `git archive`，只选择 `.agents`、`plugins`、`AGENTS.md`、`README.md`。

完整 `plugins/` 策略有意保留 32 个 tests/benchmark 源文件；这些是 tracked 源码，不是运行产物。默认 candidate benchmark 没有启用。ZIP 不含 `.git`、`docs`、`dist`、`node_modules`、日志、coverage、任务 manifest/request、owner/recovery 或临时目录。

## 固定提交上的验证

运行时：

- Node `v24.19.0`
- Python `3.12.13`
- `XHS_BUNDLED_PYTHON` 固定到 bundled Python
- 全部测试使用合成或脱敏 fixture

结果：

| 组 | 命令口径 | 结果 | duration |
| --- | --- | --- | ---: |
| 发放 | 全部 `tests/disbursement-*.test.mjs`，`node --test --test-concurrency=1 --test-reporter=spec` | 137 pass / 0 fail / 0 skip | 41646.746 ms |
| 普通报销与共享基础设施 | 其余全部 `tests/*.test.mjs`，相同 runner 参数 | 386 pass / 0 fail / 2 skip | 367841.7422 ms |
| 合计 | 27 个 test 文件 | 523 pass / 0 fail / 2 skip | 分组串行 |

两个 skip：

1. Windows 当前权限不支持 leaf symlink/reparse fixture；ancestor/reparse 拒绝测试通过，候选 ZIP/解压目录另做了 0-link/0-reparse 检查。
2. candidate benchmark 默认关闭；本期没有实施或宣称整体性能优化。

其他验证：

- `git diff --check`：通过。
- 37 个生产 `.mjs` 的 `node --check`：通过。
- skill-creator `quick_validate.py`：`Skill is valid!`。
- plugin-creator `validate_plugin.py`：`Plugin validation passed`。
- plugin manifest 除 version/cachebuster 外与前一提交语义相同。
- 普通 `run_reimbursement_workflow.mjs`、`build_gate_binding.mjs`、`audit_full_correspondence.mjs` 和 Gate 2 reference 相对 S00 基线无差异。
- 性能脚本与性能测试相对 S00 基线无差异。

## 共享 no-follow 安全例外

`audit_batch_manifest.mjs` 的 deferred 模式从“仍 stat 声明路径但不 hash”改为“完全不访问声明路径”，使 optional ordinary-manifest attestation 不会跟随未注册历史路径。普通 runner 也使用该 deferred 模式，因此文件存在性错误可能改在绑定 builder 阶段报告；这是一项明确接受的共享安全修正，不删除 Gate、不改变 Gate 授权口令、产物或发布链。

普通/共享 386 项回归实际覆盖 reused audit session no-follow、minimal ordinary workflow、Gate 1、Gate 2、full-correspondence、发布和恢复，结果 0 fail。

## ZIP 与解压复验

- sidecar 内容、实际 ZIP SHA 和预期 SHA 三者一致。
- ZIP 96 个文件与 source commit 的 Git blob 逐一相同：0 missing、0 extra、0 mismatch。
- 顶层严格为 `.agents`、`plugins`、`AGENTS.md`、`README.md`。
- 0 路径逃逸、0 大小写/Windows 冲突重复、0 symlink/reparse。
- 解压后再次通过 marketplace 结构、Plugin、Skill、37 个生产脚本静态检查。
- 解压候选的 v2 contract 与模板定向测试：36 pass / 0 fail / 0 skip。
- 0 本机绝对路径、0 `04_财务台账` 标记、0 明显秘密标记、0 临时/恢复残留。
- 5 个 XLSX 模板无 external links、comments、custom XML、媒体或本机路径，模板 SHA 与 manifest 一致。

## 外部只读起止指纹

S06 开始与结束用同一 `relative path + length + file SHA256` 只读算法比较：

| 对象 | files/bytes | 起始 SHA256 | 结束 SHA256 |
| --- | ---: | --- | --- |
| 当前 marketplace 根 | 88 files | `7e6f409554c2f6214f50a144c9916730ff348f8228eaf2b72084548ee866823c` | 相同 |
| 当前安装 cache | 85 files | `c2dabc31626ba63d96fb4ee90fd2301661dd61a93945f2a3dadc4e9c4e8483da` | 相同 |
| `config.toml` | 8382 bytes | `ff75b82ad5c8c0a15e445bc411334c3ff227a7418bc324edd808be4b9d40ac37` | 相同 |

独立 reviewer 另以 S00 canonical tree 算法复核：marketplace `249e1c1529b2d5a0fd6067d7b38ab085e6bc2a307d483c8b64fb7bbc77ec7187`，cache `68e5d328a9344de270cb4058ed21209c71dd642cfc6690695fb7ec4832aba298`，均与 INDEX 基线一致。外部 marketplace 和 cache 的版本仍为 `0.5.0+codex.20260822170308`，证明候选未安装。

## 验收清单

- [x] cachebuster 只有一个 `+codex.<token>`，base version 仍为 `0.5.0`。
- [x] source commit、tree、ZIP 和 sidecar 均固定。
- [x] `git diff --check`、生产 `.mjs`、Skill、Plugin validator 通过。
- [x] 当前 source commit 全量 523 项回归通过；两个 skip 原因明确。
- [x] ZIP 顶层、96 文件清单和 source commit blob 严格闭合。
- [x] ZIP SHA256 与 sidecar 相同，解压后版本正确。
- [x] 解压候选再次通过结构、Plugin、Skill、静态和定向测试。
- [x] 候选不存在真实财务数据、秘密、本机路径、临时恢复目录或 reparse。
- [x] marketplace、安装 cache 和 `config.toml` 起止指纹一致。
- [x] GPT-5.6-sol/xhigh 独立 reviewer 最终结论为 `GO`。
- [x] 最终候选路径、SHA256、source commit、测试结果和未安装状态已写回。

## 清理与最终状态

两个由 S06 创建的临时解压目录均已清理；首次长路径目录只因 Git legacy path limit 改用短路径，没有修改候选。仓库只保留 `dist` 中 ZIP 与 sidecar 两个预期未跟踪交付物。没有删除源文件、测试、文档或交付物。

第一次上下文压缩已按规则记录为 `thread_compression_count=1`；本单元未发生第二次压缩，无需继任对话。
