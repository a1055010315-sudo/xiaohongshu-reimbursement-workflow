# S05 Skill、README 与迁移交接

状态：`complete`

- Unit：`S05`
- owner：Skill / README / migration documentation
- branch：`s05-docs`
- worktree：`C:\Users\a1055\plugins\development\xhs-worktrees\S05-docs`
- base：`904ed27dcd03ee90523866f71dbaa7dbf001461d`
- HEAD：`S05_DOCS_COMMIT`（本交接所在提交，以交付的 `git rev-parse HEAD` 为准）
- 执行配置：`GPT-5.6-sol` / `xhigh`
- dirty files：提交后无
- thread_compression_count：`0`
- rollover_generation：`0`

## 已完成

1. 发放用户说明改为直接从用户明确指出的已发布报销成品或 fresh 证据、工资最终件、发放凭证和人工处置决定开始。
2. 明确 `sourceReview`、默认 v2 manifest、manifest SHA256 和严格四字段 archive request 均由当前任务内部生成；不要求用户准备原报销 manifest、publish receipt、工资 certificate 或其他内部 JSON，也不建议扫描目录寻找 sidecar。
3. 文档化 v2 缺省：`published_archive` 可无原 manifest/receipt，`fresh_evidence` 可无 attestation，salary 可无 certificate；任何已提供佐证仍必须 fresh 严格验证，错误、损坏或冲突阻塞。
4. 冻结 review-bound 信任边界：图片为 `review_bound_no_ocr`，没有冻结工资业务 schema 的工作簿为 `review_bound_no_frozen_salary_schema`。当前任务负责对明确文件进行 fresh 语义复核；插件不能被描述为机械 OCR 或机械金额解析，业务歧义必须询问，不能猜测。
5. 外部入口继续只接受 `--archive` 四字段 request；内部 manifest 默认 v2。manifest v1 仅严格兼容一个发布周期并返回 `DISBURSEMENT_MANIFEST_V1_DEPRECATED` warning，不增加 Gate 或确认口令。
6. 普通报销 Gate 1/Gate 2 完全不变；发放继续无人工 Gate、一次完成，并保留双次来源审计、candidate/stage/final 审计、TOCTOU、fsync、原子发布、恢复、最终三项复核和 allowlist 清理。
7. README 已把旧“当前候选”改为历史/开发基线记录，不虚构新版本；本期没有更新 version、打包、安装或实施整体性能优化。

## 修改文件

- `README.md`
- `docs/implementation/INDEX.md`
- `docs/implementation/handoffs/S05.md`
- `plugins/xiaohongshu-reimbursement-workflow/.codex-plugin/plugin.json`（仅描述/default prompt；version 未变）
- `plugins/xiaohongshu-reimbursement-workflow/skills/xiaohongshu-reimbursement-workflow/SKILL.md`
- `plugins/xiaohongshu-reimbursement-workflow/skills/xiaohongshu-reimbursement-workflow/agents/openai.yaml`
- `plugins/xiaohongshu-reimbursement-workflow/skills/xiaohongshu-reimbursement-workflow/references/compact-disbursement.md`
- `plugins/xiaohongshu-reimbursement-workflow/skills/xiaohongshu-reimbursement-workflow/references/disbursement-manifest-v2.md`

未修改 scripts、tests、templates、version/cachebuster、marketplace manifest/config、安装缓存或现用 Codex 配置。`docs/implementation/handoffs/MASTER.md` 留给主对话合并后统一更新。

## 验证

- skill-creator `quick_validate.py`（bundled Python `-X utf8`）：通过，`Skill is valid!`。
- plugin-creator `validate_plugin.py` 只读预检：通过，`Plugin validation passed`。
- v1-only 用户要求定向 `rg`：退出码 1、零匹配；未发现“每个报销来源必须同时绑定 manifest/receipt”、`originalManifestPath`、`publishReceiptPath`、“工资必须 certificate”或“先准备 manifest”等旧入口要求。
- v2 缺省、review-bound、弃用 warning、严格四字段、普通 Gate 1/Gate 2 和单次安全链定向 `rg`：均有预期命中。
- plugin manifest 版本基线/当前比较：均为 `0.5.0+codex.20260822170308`，未更新。
- public reference 只链接包内 `references/disbursement-manifest-v2.md` 与同一 skill 的权威 validator；候选 ZIP 不依赖仓库外部开发文档。
- `git diff --check`：通过。

没有运行生产回归：本单元仅修改文档和 Skill/UI 元数据，不修改生产代码或测试。

## 限制与下一动作

- 真实财务目录保持零读取、零枚举、零搜索、零修改。
- S05 无后续实现动作。任何版本更新、打包、安装、marketplace/cache/config 修改或整体性能优化必须进入后续明确授权单元。
